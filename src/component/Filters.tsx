import { Box, Typography } from "@mui/material";

export default function Filters() {
    return (
        <Box flex={1} sx={{marginTop:"20px"}}>
        <Typography>This is filter</Typography>
        </Box>
    )
}